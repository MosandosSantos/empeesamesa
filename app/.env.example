# ==============================================
# SAL GOISC - Environment Variables Template
# ==============================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ----------------------------------------------
# DATABASE
# ----------------------------------------------
# PostgreSQL Local
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/saldogoisc"

# PostgreSQL Cloud (Neon, Supabase, Railway, etc.)
# DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ----------------------------------------------
# AUTHENTICATION
# ----------------------------------------------
# JWT Secret - Generate a strong random string (min 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET="your-secure-jwt-secret-min-32-chars-change-in-production"

# ----------------------------------------------
# EMAIL CONFIGURATION (SMTP)
# ----------------------------------------------
EMAIL_HOST="smtp.example.com"
EMAIL_PORT="587"
EMAIL_USER="your-email@example.com"
EMAIL_PASSWORD="your-app-specific-password"
EMAIL_FROM="SAL GOISC <noreply@example.com>"

# ----------------------------------------------
# APPLICATION
# ----------------------------------------------
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# ----------------------------------------------
# OPENAI API (Opcional)
# ----------------------------------------------
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-proj-your-openai-api-key-here"

# ----------------------------------------------
# WHATSAPP - UltraMsg API (Opcional)
# ----------------------------------------------
# Get credentials at: https://ultramsg.com/
ULTRAMSG_INSTANCE_ID="your_instance_id"
ULTRAMSG_TOKEN="your_ultramsg_token"

# ----------------------------------------------
# NETLIFY DEPLOYMENT NOTES
# ----------------------------------------------
# 1. Add all these variables in Netlify Dashboard:
#    Site settings > Environment variables
#
# 2. For DATABASE_URL:
#    - Use a PostgreSQL database (Neon is recommended)
#    - Ensure connection pooling is enabled
#    - Use sslmode=require for security
#
# 3. For JWT_SECRET:
#    - Generate a NEW secret for production
#    - Use at least 32 random characters
#
# 4. For EMAIL (optional for MVP):
#    - Configure if you need email functionality
#    - Use app-specific passwords (not your main password)
#
# 5. For NEXT_PUBLIC_BASE_URL:
#    - Set to your Netlify domain (e.g., https://esferaordo.netlify.app)
#    - Or your custom domain if configured
